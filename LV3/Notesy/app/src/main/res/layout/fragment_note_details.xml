<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tv_note_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="@dimen/textSize_title"
        android:textAlignment="center"
        android:layout_marginTop="@dimen/margin_medium"
        tools:text="@tools:sample/lorem"/>
    <ImageView
        android:id="@+id/iv_note_priority"
        android:layout_width="0dp"
        android:layout_height="30dp"
        app:layout_constraintTop_toBottomOf="@id/tv_note_title"
        app:layout_constraintStart_toStartOf="@id/tv_note_title"
        app:layout_constraintEnd_toEndOf="@id/tv_note_title"
        android:contentDescription="@string/text_noteImportance_contentDescription"
        android:layout_marginTop="@dimen/margin_medium"
        tools:background="@color/yellow"/>
    <TextView
        android:id="@+id/tv_note_details"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/iv_note_priority"
        app:layout_constraintStart_toStartOf="@id/iv_note_priority"
        app:layout_constraintEnd_toEndOf="@id/iv_note_priority"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="@dimen/margin_medium"
        android:padding="@dimen/padding_medium"
        tools:text="@tools:sample/lorem/random"/>


</androidx.constraintlayout.widget.ConstraintLayout>